"use client"

import { Container, Typography } from "@mui/material"

export const Book = () => {
  return (
    <Container sx={{ py: 6 }}>
      <Typography variant="h1">Book</Typography>
    </Container>
  )
}
